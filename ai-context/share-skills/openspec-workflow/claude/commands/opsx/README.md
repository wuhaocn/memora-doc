# Projection Source

This directory is the source for the Claude `opsx` command projection.

Projected target:
- `~/.claude/commands/opsx`

Refresh projection with:

```bash
node scripts/tools/sync-share-skills.js --project-only
```
